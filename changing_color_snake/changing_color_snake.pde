void setup() {
  size(300,500); //size(width,height);
  // This is a comment.
  background(255,100,15); //colors run from 0 -255
}
// GOAL: Make color vary with speed

void draw() {  
 // circle
 ellipse(mouseX, mouseY, 100, 75); //ellipse (x,y,width,height)
 
 
}
  
